<?xml version='1.0'?>
<manifest xmlns:android='http://schemas.android.com/apk/res/android' package='giuliolunati.rebolserver'>
    <uses-sdk android:targetSdkVersion="23" />
    <uses-sdk android:minSdkVersion="16" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application android:label='Rebol Server'>
        <activity android:name='giuliolunati.rebolserver.MainActivity'>
             <intent-filter>
                <category android:name='android.intent.category.LAUNCHER'/>
                <action android:name='android.intent.action.MAIN'/>
             </intent-filter>
        </activity>
        <service android:name=".Server" />
    </application>
</manifest>
